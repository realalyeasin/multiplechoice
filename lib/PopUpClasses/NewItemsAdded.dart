import 'package:flutter/material.dart';

class NewItemsAdded extends StatelessWidget {
  const NewItemsAdded({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.teal,
    );
  }
}
